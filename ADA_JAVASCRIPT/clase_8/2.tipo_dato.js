// Definir distintos tipos de datos
let numero = 42; // Numero entero
let numero2 = 26.89 // Numero decimal
let texto = 'Hola chicas' // se lla ma string (puede ir con collias simples)
let booleano = true; // Puede ser false 
let indefinido; // me dara undefined
let nulo = null; // valor vacio

console.log( `Numeros: ${numero} y ${numero2}`);
console.log( `Texto: ${texto}`);
console.log( `Booleano: ${booleano}`);
console.log( `Indefinido: ${indefinido}`);
console.log( `Nulo: ${nulo}`);

